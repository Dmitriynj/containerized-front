const API_URL = process.env.API_URL;

const DATE_TIME_PATTERN = 'YYYY-MM-DD HH:mm:ss';
const EMAIL_PATTERN = /^\w+([\.-]?\w+)*@\w+([\.-]?\w+)*(\.\w{2,3})+$/;

export { API_URL, DATE_TIME_PATTERN, EMAIL_PATTERN };
