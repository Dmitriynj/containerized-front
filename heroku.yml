build:
  docker:
    web: Dockerfile
  config:
    NODE_ENV: production
    API_URL: https://containerized-back.herokuapp.com/api
    PORT: 3000
    APP_ID: some
    API_KEY: some
run:
  web: npm start